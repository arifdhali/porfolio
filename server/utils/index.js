const SlugGenerator = require('./Slug_genrate');
const CurrentDate = require('./date.utils');
const Multer = require('./Multer.config');
const QueryReducer = require('./Table_Query_Reducer');


module.exports = {
    CurrentDate,
    SlugGenerator,
    Multer,
    QueryReducer

}